\* Definition-oriented entry cfg for the exact setup wire spec.
\* This is intentionally not a runnable TLC harness: BoomerangSetupWire.tla
\* preserves the setup design's full ordered WT/SAR collections and 1..195
\* duress permutation space rather than introducing tractability abstractions.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
\* Add statements after this line.
